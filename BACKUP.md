# Dotfiles backup

The directories in this repository mirror directories under `~/.config`.
Files beginning with a dot at the repository root mirror files in `$HOME`.

Neovim is intentionally excluded because `~/.config/nvim` has its own Git
repository. Credentials, session tokens, caches, and logs must not be added.

